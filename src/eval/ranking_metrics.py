"""Stable package import surface for ranking evaluation metrics.

The implementation is shared with the command-line analysis script under
``scripts/`` so existing script invocations and package imports stay aligned.
"""

from scripts.ranking_metrics import *  # noqa: F401,F403

