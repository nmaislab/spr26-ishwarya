# Archival API Inventory Reports

ARCHIVAL: these CSV files are not read by the current AutoLLMCompose runtime
pipeline.

They are ToolBench endpoint count reports produced by the historical
`data/data_gen/api_counts_by_category.ipynb` notebook:

- `toolbench_api_counts.by_category.csv`
- `toolbench_api_counts.by_file.csv`

Keep these files for transparency and thesis reproducibility. They are reference
reports only; changing or regenerating them does not affect normal pipeline
runs.
